name: Keep Warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check
        run: curl -fsS --max-time 30 --retry 2 --retry-delay 5 https://lms-kw2e.onrender.com/up
